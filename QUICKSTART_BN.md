# 🚀 Unique Notify - দ্রুত শুরু করার গাইড (বাংলা)

**Unique Notify** হলো cPanel/WHM-এর জন্য একটি CloudLinux CPU মনিটরিং প্লাগিন যা টেলিগ্রামে সতর্কতা পাঠায়।

---

## 📥 ইনস্টলেশন

### সরাসরি ইনস্টল করুন (একটি কমান্ড):

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/noyonmiahdev/Unique-Notify/main/install.sh)
```

**নোট:** যদি 404 এরর দেখায়, তাহলে `master` ব্যবহার করুন:
```bash
bash <(curl -fsSL https://raw.githubusercontent.com/noyonmiahdev/Unique-Notify/master/install.sh)
```

---

## ⚙️ সেটআপ করুন

### ১. Telegram Bot তৈরি করুন

1. টেলিগ্রামে যান এবং **@BotFather** খুঁজুন
2. `/newbot` কমান্ড পাঠান
3. বট এর নাম দিন (উদাহরণ: "My Server Monitor")
4. একটি ইউজারনেম দিন (অবশ্যই `bot` দিয়ে শেষ হতে হবে)
5. আপনি একটি **Bot Token** পাবেন (উদাহরণ: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### ২. Chat ID পান

1. আপনার বটে `/start` পাঠান
2. এই লিংকে যান (আপনার Token দিয়ে):
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
3. `"chat":{"id":` এর পরে আপনার Chat ID পাবেন (উদাহরণ: `123456789`)

### ৩. WHM-এ কনফিগার করুন

1. WHM লগইন করুন
2. **Plugins → Unique Notify** এ যান
3. তথ্য পূরণ করুন:
   - **Bot Token**: আপনার Telegram Bot Token
   - **Chat ID**: আপনার Chat ID
   - **CPU Threshold**: কত শতাংশ CPU ব্যবহার হলে সতর্কতা পাবেন (যেমন: 90)
   - **Cooldown**: একই ইউজারের জন্য পরবর্তী সতর্কতার আগে কত মিনিট অপেক্ষা করবে (যেমন: 30)

### ৪. টেস্ট করুন

1. **📤 Test Telegram** বাটনে ক্লিক করুন
2. আপনার টেলিগ্রামে একটি টেস্ট মেসেজ আসবে
3. সফল হলে: "✅ Test message sent successfully!" দেখাবে
4. ব্যর্থ হলে: আপনার Bot Token এবং Chat ID চেক করুন

### ৫. সংরক্ষণ করুন

1. **💾 Save Configuration** বাটনে ক্লিক করুন
2. সফল হলে সবুজ মেসেজ দেখাবে
3. **↻ Restart Service** বাটনে ক্লিক করুন

---

## 🔄 আপডেট করুন

প্লাগিন আপডেট করতে এই কমান্ড চালান:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/noyonmiahdev/Unique-Notify/main/update.sh)
```

**নোট:** যদি 404 এরর দেখায়, তাহলে `master` ব্যবহার করুন:
```bash
bash <(curl -fsSL https://raw.githubusercontent.com/noyonmiahdev/Unique-Notify/master/update.sh)
```

**আপডেট করলে কি হয়:**
- ✅ আপনার সেটিংস সংরক্ষিত থাকবে
- ✅ নতুন ফিচার এবং বাগ ফিক্স পাবেন
- ✅ সার্ভিস স্বয়ংক্রিয়ভাবে রিস্টার্ট হবে
- ✅ একটি ব্যাকআপ তৈরি হবে

---

## 🧪 টেলিগ্রাম টেস্ট করার বিকল্প পদ্ধতি

WHM প্যানেল থেকে ছাড়াও, আপনি কমান্ড লাইন থেকে টেস্ট করতে পারেন:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/sendMessage" \
     -d "chat_id=<YOUR_CHAT_ID>" \
     -d "text=টেস্ট মেসেজ - Unique Notify"
```

**উদাহরণ:**
```bash
curl -X POST "https://api.telegram.org/bot1234567890:ABCdefGHIjklMNOpqrsTUVwxyz/sendMessage" \
     -d "chat_id=123456789" \
     -d "text=টেস্ট মেসেজ - Unique Notify"
```

---

## 📡 সতর্কতা মেসেজ কেমন দেখাবে

যখন কোনো ইউজারের CPU ব্যবহার বেশি হবে, তখন এই ধরনের মেসেজ পাবেন:

```
⚠️ High CPU Alert
Host: server01.example.com
User: johndoe
CPU: 95.8% ≥ 90%
Time: 2025-11-10 15:42:23
```

---

## 🔍 সমস্যা সমাধান

### ১. প্লাগিন দেখা যাচ্ছে না

**সমাধান:**
```bash
/usr/local/cpanel/bin/register_appconfig /var/cpanel/apps/uniquenotify.conf
systemctl restart uniquenotify.service
```

### ২. টেলিগ্রাম মেসেজ পাঠাচ্ছে না

**চেক করুন:**
- Bot Token সঠিক আছে কিনা
- Chat ID সঠিক আছে কিনা
- Internet connection ঠিক আছে কিনা

**লগ দেখুন:**
```bash
journalctl -u uniquenotify.service -f
```

### ৩. সার্ভিস চালু নেই

**সার্ভিস চালু করুন:**
```bash
systemctl start uniquenotify.service
systemctl status uniquenotify.service
```

### ৪. CloudLinux LVE নেই

**Warning দেখলে:**
- এটি শুধুমাত্র CloudLinux সার্ভারের জন্য
- অন্য সার্ভারে CPU মনিটরিং সীমিত হবে

---

## 🛠️ দরকারী কমান্ড

### সার্ভিস ম্যানেজমেন্ট

```bash
# স্ট্যাটাস দেখুন
systemctl status uniquenotify.service

# লগ দেখুন
journalctl -u uniquenotify.service -f

# রিস্টার্ট করুন
systemctl restart uniquenotify.service

# বন্ধ করুন
systemctl stop uniquenotify.service

# চালু করুন
systemctl start uniquenotify.service
```

### কনফিগারেশন

```bash
# কনফিগ ফাইল এডিট করুন
nano /var/cpanel/uniquenotify/config.json

# কনফিগ দেখুন
cat /var/cpanel/uniquenotify/config.json
```

### আনইনস্টল

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/noyonmiahdev/Unique-Notify/main/uninstall.sh)
```

---

## 📞 সাপোর্ট

**সমস্যা হলে:**
1. [GitHub Issues](https://github.com/noyonmiahdev/Unique-Notify/issues) এ রিপোর্ট করুন
2. লগ ফাইল দেখুন: `/var/cpanel/uniquenotify/daemon.log`
3. সার্ভিস স্ট্যাটাস চেক করুন: `systemctl status uniquenotify.service`

**GitHub:** [https://github.com/noyonmiahdev/Unique-Notify](https://github.com/noyonmiahdev/Unique-Notify)

---

## ✅ চেকলিস্ট

- [ ] প্লাগিন ইনস্টল করেছেন
- [ ] Telegram Bot তৈরি করেছেন
- [ ] Bot Token এবং Chat ID পেয়েছেন
- [ ] WHM-এ কনফিগারেশন করেছেন
- [ ] টেলিগ্রাম টেস্ট করেছেন (সফল)
- [ ] কনফিগারেশন সংরক্ষণ করেছেন
- [ ] সার্ভিস চালু আছে

---

**ধন্যবাদ Unique Notify ব্যবহার করার জন্য! 🎉**
